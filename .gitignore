node_modules/
dist/
.env
*.log
.DS_Store
devlog/
.tmp/
.opencode/

# Local agent/session artifacts
# These are per-machine agent state (goalplans, ledgers, evidence scratch).
# They are never part of the product and must not be committed, not even with
# `git add -f` — see tests/repo-hygiene.test.ts, which fails if any path here
# becomes tracked again.
.codexclaw/
**/.codexclaw/
.omo/
**/.omo/

# Test-generated artifacts
tests/.tmp-*/
.claude/

# Local Cursor/Claude agent skill mirrors and session scratch (never product).
# Same class of leak as `.claude/` / `.codexclaw/` — see tests/repo-hygiene.test.ts.
.agents/
**/.agents/

# stray files a broad 'git add' swept in (not part of the product)
.idea/amazonq.xml
ddd/
/*.png

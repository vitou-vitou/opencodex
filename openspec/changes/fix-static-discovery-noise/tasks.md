## 1. OpenSpec

- [x] 1.1 proposal / design / spec / tasks

## 2. Registry + comments

- [x] 2.1 Set `liveModels: false` on `kimi`, `kimi-code`, `google-antigravity` (+ brief notes)
- [x] 2.2 Sync `buildModelsRequest` antigravity comment in `src/oauth/index.ts`

## 3. Tests

- [x] 3.1 Assert registry `liveModels: false` for the three providers
- [x] 3.2 Assert `fetchProviderModels` with `liveModels: false` skips fetch

## 4. Verify

- [x] 4.1 typecheck + focused tests
- [x] 4.2 Aikido on first-party edits
